Hook's name:

- Hook using Tanstack Query:
  - get:
    - get detail: use + Get + N(singular) + Query
    - get list: use + Get + N(plural) + Query
  - create:
    - use + Create + N(singular) + Mutation

- Component slot: slot name + Slot
